{% extends "Admin/base.html.twig" %}

{% block title %}Modifier le commentaire{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier le commentaire <small>&laquo; {{ comment.pseudo }} &raquo;</small></h1>
        <hr>
        <p class="text-right">
            <a href="{{ path('admin_comment_index') }}" class="btn btn-sm btn-default">
                Retour à la liste
            </a>
        </p>
        {{ form(form) }}
    </div>
{% endblock %}
